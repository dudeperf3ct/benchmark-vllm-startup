apiVersion: v1
kind: Service
metadata:
  name: vllm-baseline
  namespace: llm-bench
spec:
  type: ClusterIP
  selector:
    app: vllm-baseline
  ports:
    - name: http
      port: 8000
      targetPort: 8000
